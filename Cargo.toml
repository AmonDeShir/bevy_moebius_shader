[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[package]
name = "bevy_shaders"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.14.2", features = ["default", "file_watcher"] }
bevy-inspector-egui = "0.26.0"
blenvy = "0.1.0-alpha.1"
